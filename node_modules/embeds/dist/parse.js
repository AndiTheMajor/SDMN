'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _lodash = require('lodash.values');

var _lodash2 = _interopRequireDefault(_lodash);

var _types2 = require('./types');

var _types3 = _interopRequireDefault(_types2);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var types = (0, _lodash2.default)(_types3.default);

exports.default = function (elements) {
  if (elements && elements.nodeName) {
    elements = [elements];
  }

  if (elements && elements.length) {
    elements = Array.prototype.slice.call(elements);

    for (var i = 0; i < types.length; i++) {
      var type = types[i];
      var results = type.parse(elements);
      if (results) {
        return results;
      }
    }
  }
  return null;
};